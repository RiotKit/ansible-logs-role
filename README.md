RiotKit's logging role
======================

Basic logging configuration with target to optimize disk space usage.

Features:
- Log rotation (uses external role debops.lograte)
- Systemd logging limits

Requires:
- debops.logrotate role

